# Environment file for Hanabi GPU Server
# Copy this to /etc/default/hanabi-gpu-server on your laptop

# Port to listen on
GPU_PORT=5555

# Shared authentication token (set the same on both machines)
AUTH_TOKEN=your-secret-token-change-me

# Optional: Specify GPU device (auto, cuda:0, cuda:1, etc.)
# DEVICE=auto
