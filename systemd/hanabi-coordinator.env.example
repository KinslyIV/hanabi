# Environment file for Hanabi Coordinator
# Copy this to /etc/default/hanabi-coordinator on your server

# GPU server (laptop) connection details
GPU_HOST=192.168.1.100   # Change to your laptop's IP
GPU_PORT=5555

# Shared authentication token (set the same on both machines)
AUTH_TOKEN=your-secret-token-change-me

# Training configuration
NUM_ITERATIONS=1000
GAMES_PER_ITERATION=100
TRAIN_STEPS=500
BATCH_SIZE=256

# WandB (optional)
# Set to "--use-wandb" to enable, or leave empty to disable
WANDB_FLAGS="--use-wandb --wandb-project hanabi-distributed"
